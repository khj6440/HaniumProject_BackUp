import React from "react";
import SnackPresenter from "./SnackPresenter";

export default class SnackContainer extends React.Component {
  state = {
  };
  render() {
    return <SnackPresenter />;
  }
}
